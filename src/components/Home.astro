---
import { linkWhatsapp } from "../data/contact";
import Button from "./ui/Button.astro";
---

<header class="h-svh flex justify-center items-center">
  <div
    class="flex flex-col justify-center items-center md:gap-12 gap-6 md:p-12 p-6 relative"
  >
    <h1
      class="title text-slate-200 inline-block md:text-7xl text-3xl font-bold text-center text-balance"
    >
      Lleva tu negocio al siguiente nivel
    </h1>
    <p class="subtitle text-slate-400 md:text-2xl text-xl text-center">
      Diseños web personalizados que reflejan la esencia de tu marca
    </p>
    <footer class="footer">
      <Button href={linkWhatsapp}>Obtén tu Cotización Gratis</Button>
    </footer>
    <div
      class="absolute -z-50 bottom-0 left-96 bg-purple-500/20 w-96 h-96 rounded-full blur-[100px]"
    >
    </div>
    <div
      class="absolute -z-50 top-0 right-20 bg-violet-500/20 w-96 h-96 rounded-full blur-[100px]"
    >
    </div>
  </div>
</header>

<style>
  .title {
    animation: blow 0.6s both ease-out;
  }

  @keyframes blow {
    0% {
      filter: blur(20px);
      transform: translateY(20px);
      opacity: 0;
    }
  }

  .subtitle {
    animation: blow 0.6s both 0.2s ease-out;
  }

  .footer {
    animation: blow 0.6s both 0.4s ease-out;
  }
</style>
